name: Evotech Bot

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run Evotech Bot
        uses: yossi-asher/evotech-bot@v1.0.0
        with:
          token: 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

